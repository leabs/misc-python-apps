[project]
name = "quick-cms-add"
version = "0.1.0"
description = "Lightweight macOS helper for importing XLSX rows into a Strapi form."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
